- name: Install Docker
  become: true
  ansible.builtin.apt:
    install_recommends: false
    name:
      - docker.io
      - docker-compose
    state: present

- name: Clean up Docker data
  become: true
  community.docker.docker_prune:
    containers: true
    images: true
    images_filters:
      dangling: false
    networks: true
    volumes: true
    builder_cache: true
