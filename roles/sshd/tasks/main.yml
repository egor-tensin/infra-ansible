- name: Get host distro
  ansible.builtin.setup:
    gather_subset:
      - distribution
      - distribution_release

- name: Set platform-specific variables
  ansible.builtin.include_vars: "{{ __sshd_vars_file }}"
  loop:
    - "{{ ansible_facts['distribution'] }}.yml"
    - "{{ ansible_facts['distribution'] }}_{{ ansible_facts['distribution_release'] }}.yml"
  vars:
    __sshd_vars_file: "{{ role_path }}/vars/{{ item }}"
  when: __sshd_vars_file is exists

- name: Configure sshd
  become: true
  ansible.builtin.template:
    src: sshd_config.j2
    dest: /etc/ssh/sshd_config
    owner: root
    group: root
    mode: '644'
    validate: /usr/sbin/sshd -t -f %s
  register: sshd_conf

- name: Restart sshd
  become: true
  when: sshd_conf is changed
  ansible.builtin.systemd_service:
    name: '{{ __sshd_service_name }}'
    state: restarted

- name: Enable sshd service
  become: true
  ansible.builtin.systemd_service:
    name: '{{ __sshd_service_name }}.service'
    enabled: true
